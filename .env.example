# Environment variables for the Discord bot
DISCORD_TOKEN=

# docker compose up で起動する場合は "db"、VSCode デバッガーで直接実行する場合は "localhost"
DATABASE_URL=postgres://bot:botpass@db:5432/botdb?sslmode=disable

# PostgreSQL 設定
POSTGRES_USER=bot
POSTGRES_PASSWORD=botpass
POSTGRES_DB=botdb
